map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 8000;
    server_name _;

    resolver 168.63.129.16 valid=30s ipv6=off;

    client_max_body_size 50m;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_ssl_server_name on;

    location = /health {
        default_type application/json;
        return 200 '{"status":"ok","service":"api-gateway"}';
    }

    location = /robots933456.txt {
        default_type text/plain;
        return 200 "ok\n";
    }

    location /rh/interviews {
        proxy_set_header Host rh-calendar-service.azurewebsites.net;
        proxy_ssl_name rh-calendar-service.azurewebsites.net;
        proxy_pass ${CALENDAR_SERVICE_URL};
    }

    location /rh/dashboard {
        proxy_set_header Host rh-analytics-service.azurewebsites.net;
        proxy_ssl_name rh-analytics-service.azurewebsites.net;
        proxy_pass ${ANALYTICS_SERVICE_URL};
    }

    location /rh/stt {
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_set_header Host rh-media-service.azurewebsites.net;
        proxy_ssl_name rh-media-service.azurewebsites.net;
        proxy_pass ${MEDIA_SERVICE_URL};
    }

    location /rh/tts {
        proxy_set_header Host rh-media-service.azurewebsites.net;
        proxy_ssl_name rh-media-service.azurewebsites.net;
        proxy_pass ${MEDIA_SERVICE_URL};
    }

    location /rh/vision/config {
        proxy_set_header Host rh-media-service.azurewebsites.net;
        proxy_ssl_name rh-media-service.azurewebsites.net;
        proxy_pass ${MEDIA_SERVICE_URL};
    }

    location /ws/rh/stt {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host rh-media-service.azurewebsites.net;
        proxy_ssl_name rh-media-service.azurewebsites.net;
        proxy_pass ${MEDIA_SERVICE_URL};
    }

    location ~ ^/rh/sessions/[^/]+/(vision|audio|proctoring)$ {
        proxy_set_header Host rh-media-service.azurewebsites.net;
        proxy_ssl_name rh-media-service.azurewebsites.net;
        proxy_pass ${MEDIA_SERVICE_URL};
    }

    location ~ ^/rh/sessions/[^/]+/(report\.pdf|insights-report\.pdf)$ {
        proxy_set_header Host rh-reporting-service.azurewebsites.net;
        proxy_ssl_name rh-reporting-service.azurewebsites.net;
        proxy_pass ${REPORTING_SERVICE_URL};
    }

    location /ws/rh/ {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host rh-interview-service.azurewebsites.net;
        proxy_ssl_name rh-interview-service.azurewebsites.net;
        proxy_pass ${INTERVIEW_SERVICE_URL};
    }

    location /rh/sessions {
        proxy_set_header Host rh-interview-service.azurewebsites.net;
        proxy_ssl_name rh-interview-service.azurewebsites.net;
        proxy_pass ${INTERVIEW_SERVICE_URL};
    }

    location / {
        proxy_set_header Host rh-interview-service.azurewebsites.net;
        proxy_ssl_name rh-interview-service.azurewebsites.net;
        proxy_pass ${INTERVIEW_SERVICE_URL};
    }
}
