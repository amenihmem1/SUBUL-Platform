/**
 * SMTP test lives under backend/api (nodemailer is installed there).
 *
 *   cd backend/api
 *   npm run test:smtp
 *
 * Set SMTP_USER, SMTP_PASS, MAIL_FROM (same as Nest MailService).
 */
console.error('Use: cd backend/api && npm run test:smtp');
process.exit(1);
