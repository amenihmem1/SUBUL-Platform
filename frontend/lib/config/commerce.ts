/**
 * When false, learner-facing Standard/Premium checkout upgrade CTAs are hidden
 * (sidebar promos, dashboard upgrade strips, locked-overlay checkout links, etc.).
 * Set to true to restore upgrade commerce in the UI.
 */
export const ENABLE_LEARNER_CHECKOUT_UPGRADE_CTAS = false;
